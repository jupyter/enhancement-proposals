version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
    nodejs: "22"
  commands:
    - pip install nox
    - nox -s docs
    # mystmd writes the site to _build/html; hand it to Read the Docs.
    - mkdir -p $READTHEDOCS_OUTPUT/html
    - cp -r _build/html/. $READTHEDOCS_OUTPUT/html
